{
  "extends": "tslint-config-airbnb",
  "rules": {
    "max-line-length": {
      "options": [
        {"limit": 120, "ignore-pattern": "^import |^export {(.*?)}"}
      ]
    }
  },
  "jsRules": {
    "max-line-length": {
      "options": [
        {"limit": 120, "ignore-pattern": "^import |^export {(.*?)}"}
      ]
    }
  }
}
